// import { FileList, TreeItem } from '../models/tree'
// export const generateTreeFromFileList = (basePath: string, list: FileList): TreeItem[] => {
//   return [{
//     name: getRootPathToDisplay(list),
//     fullPath: basePath,
//     children: generateChildrenFromFileList(basePath, list)
//   }]
// }

// const generateChildrenFromFileList = (basePath: string, list: FileList): TreeItem[] => {
//   return []
// }

// const getRootPathToDisplay = (list: FileList): string => list?.[0]?.[0] || ''
